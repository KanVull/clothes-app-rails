<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <%= link_to "Store", catalog_path, class: 'navbar-brand'%>
    <%= render partial: "shared/admin_button" %>
    <div class="navbar-nav btn-group ml-auto">
      <%= render partial: "shared/cart", locals: { cart: current_cart } %>
      <%= render partial: "shared/profile_navbar", locals: { user: current_user } %>
    </div>
  </div>
</nav>
<div class='mb-5'></div>